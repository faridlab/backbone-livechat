//! Domain events for ChatbotAnswer entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use crate::domain::entity::ChatbotAnswer;
use backbone_messaging::CrudEvent;

/// Domain events for ChatbotAnswer — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<ChatbotAnswer>)` variant.
pub type ChatbotAnswerEvent = CrudEvent<ChatbotAnswer>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for ChatbotAnswer events.
pub type ChatbotAnswerEventPublisher = Arc<dyn CrudEventPublisher<ChatbotAnswer> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopChatbotAnswerEventPublisher = NoOpCrudEventPublisher<ChatbotAnswer>;
