//! Domain events for MemberHistory entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::MemberHistory;

/// Domain events for MemberHistory — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<MemberHistory>)` variant.
pub type MemberHistoryEvent = CrudEvent<MemberHistory>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for MemberHistory events.
pub type MemberHistoryEventPublisher = Arc<dyn CrudEventPublisher<MemberHistory> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopMemberHistoryEventPublisher = NoOpCrudEventPublisher<MemberHistory>;
