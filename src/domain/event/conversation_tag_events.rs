//! Domain events for ConversationTag entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::ConversationTag;

/// Domain events for ConversationTag — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<ConversationTag>)` variant.
pub type ConversationTagEvent = CrudEvent<ConversationTag>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for ConversationTag events.
pub type ConversationTagEventPublisher = Arc<dyn CrudEventPublisher<ConversationTag> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopConversationTagEventPublisher = NoOpCrudEventPublisher<ConversationTag>;
