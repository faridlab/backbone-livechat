//! Domain events for Channel entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Channel;

/// Domain events for Channel — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Channel>)` variant.
pub type ChannelEvent = CrudEvent<Channel>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Channel events.
pub type ChannelEventPublisher = Arc<dyn CrudEventPublisher<Channel> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopChannelEventPublisher = NoOpCrudEventPublisher<Channel>;
