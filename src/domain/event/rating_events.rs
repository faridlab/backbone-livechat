//! Domain events for Rating entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use crate::domain::entity::Rating;
use backbone_messaging::CrudEvent;

/// Domain events for Rating — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Rating>)` variant.
pub type RatingEvent = CrudEvent<Rating>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Rating events.
pub type RatingEventPublisher = Arc<dyn CrudEventPublisher<Rating> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopRatingEventPublisher = NoOpCrudEventPublisher<Rating>;
