//! Domain events for ChatbotScript entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use crate::domain::entity::ChatbotScript;
use backbone_messaging::CrudEvent;

/// Domain events for ChatbotScript — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<ChatbotScript>)` variant.
pub type ChatbotScriptEvent = CrudEvent<ChatbotScript>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for ChatbotScript events.
pub type ChatbotScriptEventPublisher = Arc<dyn CrudEventPublisher<ChatbotScript> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopChatbotScriptEventPublisher = NoOpCrudEventPublisher<ChatbotScript>;
