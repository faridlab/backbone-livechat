//! Domain events for ChannelMember entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::ChannelMember;

/// Domain events for ChannelMember — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<ChannelMember>)` variant.
pub type ChannelMemberEvent = CrudEvent<ChannelMember>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for ChannelMember events.
pub type ChannelMemberEventPublisher = Arc<dyn CrudEventPublisher<ChannelMember> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopChannelMemberEventPublisher = NoOpCrudEventPublisher<ChannelMember>;
