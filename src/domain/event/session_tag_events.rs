//! Domain events for SessionTag entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::SessionTag;

/// Domain events for SessionTag — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<SessionTag>)` variant.
pub type SessionTagEvent = CrudEvent<SessionTag>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for SessionTag events.
pub type SessionTagEventPublisher = Arc<dyn CrudEventPublisher<SessionTag> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopSessionTagEventPublisher = NoOpCrudEventPublisher<SessionTag>;
