//! Specifications for ConversationTag aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Entity-specific predicate structs implementing `backbone_core::Specification`.
//! Combinators (AND/OR/NOT) are available via the trait methods — no duplication.

use crate::domain::entity::ConversationTag;
use backbone_core::Specification;

pub use backbone_core::{AndSpecification, NotSpecification, OrSpecification};

// ─── Entity-specific predicates ──────────────────────────────────────────────
//
// Add entity-specific predicate structs below.
// Each predicate implements `Specification<ConversationTag>` and can be combined
// using `.and()`, `.or()`, `.not()` from the trait.
//
// Example:
//   let spec = ActiveSpec.and(VerifiedSpec);
//   if spec.is_satisfied_by(&entity) { ... }

// <<< CUSTOM
// Add predicate structs here. Example:
//
// pub struct ActiveConversationTagSpec;
// impl Specification<ConversationTag> for ActiveConversationTagSpec {
//     fn is_satisfied_by(&self, e: &ConversationTag) -> bool {
//         // e.is_active
//         todo!()
//     }
// }
// END CUSTOM
