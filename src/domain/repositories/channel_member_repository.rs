//! Repository trait for ChannelMember aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the ChannelMember aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::ChannelMember;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ChannelMemberPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl ChannelMemberPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct ChannelMemberPaginatedResult {
    pub data: Vec<ChannelMember>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ChannelMemberFilter {
    pub channel_id: Option<Uuid>,
    pub user_id: Option<Uuid>,
}

impl ChannelMemberFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.channel_id.is_some() || self.user_id.is_some()
    }
}

/// Repository trait for ChannelMember aggregate
///
/// This trait defines the persistence contract for ChannelMember entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait ChannelMemberRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new channel_member entity
    async fn save(&self, entity: &ChannelMember) -> Result<ChannelMember>;

    /// Find channel_member by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<ChannelMember>>;

    /// Find all channel_member entities
    async fn find_all(&self) -> Result<Vec<ChannelMember>>;

    /// Update channel_member by ID
    async fn update(&self, id: &str, entity: &ChannelMember) -> Result<Option<ChannelMember>>;

    /// Delete channel_member by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List channel_member with pagination
    async fn list(&self, params: ChannelMemberPaginationParams) -> Result<ChannelMemberPaginatedResult>;

    /// List channel_member with pagination and filters
    async fn list_with_filters(&self, params: ChannelMemberPaginationParams, filters: ChannelMemberFilter) -> Result<ChannelMemberPaginatedResult>;

    /// Count all channel_member entities
    async fn count(&self) -> Result<u64>;

    /// Count channel_member entities matching filters
    async fn count_with_filters(&self, filters: ChannelMemberFilter) -> Result<u64>;

    /// Check if channel_member exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete channel_member by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted channel_member by ID
    async fn restore(&self, id: &str) -> Result<Option<ChannelMember>>;

    /// List soft-deleted channel_member entities
    async fn list_deleted(&self, params: ChannelMemberPaginationParams) -> Result<ChannelMemberPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save channel_member entities
    async fn bulk_save(&self, entities: &[ChannelMember]) -> Result<Vec<ChannelMember>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
