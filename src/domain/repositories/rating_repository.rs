//! Repository trait for Rating aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Rating aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{Rating, LivechatRatedPersona};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RatingPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl RatingPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct RatingPaginatedResult {
    pub data: Vec<Rating>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RatingFilter {
    pub session_id: Option<Uuid>,
    pub rated_persona: Option<LivechatRatedPersona>,
    pub operator_user_id: Option<Uuid>,
    pub chatbot_script_id: Option<Uuid>,
    pub comment: Option<String>,
}

impl RatingFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.session_id.is_some() || self.rated_persona.is_some() || self.operator_user_id.is_some() || self.chatbot_script_id.is_some() || self.comment.is_some()
    }
}

/// Repository trait for Rating aggregate
///
/// This trait defines the persistence contract for Rating entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait RatingRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new rating entity
    async fn save(&self, entity: &Rating) -> Result<Rating>;

    /// Find rating by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Rating>>;

    /// Find all rating entities
    async fn find_all(&self) -> Result<Vec<Rating>>;

    /// Update rating by ID
    async fn update(&self, id: &str, entity: &Rating) -> Result<Option<Rating>>;

    /// Delete rating by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List rating with pagination
    async fn list(&self, params: RatingPaginationParams) -> Result<RatingPaginatedResult>;

    /// List rating with pagination and filters
    async fn list_with_filters(&self, params: RatingPaginationParams, filters: RatingFilter) -> Result<RatingPaginatedResult>;

    /// Count all rating entities
    async fn count(&self) -> Result<u64>;

    /// Count rating entities matching filters
    async fn count_with_filters(&self, filters: RatingFilter) -> Result<u64>;

    /// Check if rating exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete rating by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted rating by ID
    async fn restore(&self, id: &str) -> Result<Option<Rating>>;

    /// List soft-deleted rating entities
    async fn list_deleted(&self, params: RatingPaginationParams) -> Result<RatingPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find rating by session_id
    async fn find_by_session_id(&self, session_id: Uuid) -> Result<Option<Rating>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if rating exists by session_id
    async fn exists_by_session_id(&self, session_id: Uuid) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save rating entities
    async fn bulk_save(&self, entities: &[Rating]) -> Result<Vec<Rating>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
