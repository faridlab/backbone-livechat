//! Repository trait for ExpertiseTag aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the ExpertiseTag aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::ExpertiseTag;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ExpertiseTagPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl ExpertiseTagPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct ExpertiseTagPaginatedResult {
    pub data: Vec<ExpertiseTag>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ExpertiseTagFilter {
    pub name: Option<String>,
}

impl ExpertiseTagFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some()
    }
}

/// Repository trait for ExpertiseTag aggregate
///
/// This trait defines the persistence contract for ExpertiseTag entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait ExpertiseTagRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new expertise_tag entity
    async fn save(&self, entity: &ExpertiseTag) -> Result<ExpertiseTag>;

    /// Find expertise_tag by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<ExpertiseTag>>;

    /// Find all expertise_tag entities
    async fn find_all(&self) -> Result<Vec<ExpertiseTag>>;

    /// Update expertise_tag by ID
    async fn update(&self, id: &str, entity: &ExpertiseTag) -> Result<Option<ExpertiseTag>>;

    /// Delete expertise_tag by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List expertise_tag with pagination
    async fn list(&self, params: ExpertiseTagPaginationParams) -> Result<ExpertiseTagPaginatedResult>;

    /// List expertise_tag with pagination and filters
    async fn list_with_filters(&self, params: ExpertiseTagPaginationParams, filters: ExpertiseTagFilter) -> Result<ExpertiseTagPaginatedResult>;

    /// Count all expertise_tag entities
    async fn count(&self) -> Result<u64>;

    /// Count expertise_tag entities matching filters
    async fn count_with_filters(&self, filters: ExpertiseTagFilter) -> Result<u64>;

    /// Check if expertise_tag exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete expertise_tag by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted expertise_tag by ID
    async fn restore(&self, id: &str) -> Result<Option<ExpertiseTag>>;

    /// List soft-deleted expertise_tag entities
    async fn list_deleted(&self, params: ExpertiseTagPaginationParams) -> Result<ExpertiseTagPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save expertise_tag entities
    async fn bulk_save(&self, entities: &[ExpertiseTag]) -> Result<Vec<ExpertiseTag>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
