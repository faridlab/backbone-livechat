//! Repository trait for LivechatAuditLog aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the LivechatAuditLog aggregate.
//! Implementation is in the infrastructure layer.

use anyhow::Result;
use async_trait::async_trait;
use uuid::Uuid;

use crate::domain::entity::{LivechatAuditEvent, LivechatAuditLog};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LivechatAuditLogPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl LivechatAuditLogPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct LivechatAuditLogPaginatedResult {
    pub data: Vec<LivechatAuditLog>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LivechatAuditLogFilter {
    pub event: Option<LivechatAuditEvent>,
    pub actor: Option<Uuid>,
    pub subject_type: Option<String>,
    pub subject_id: Option<Uuid>,
    pub company_id: Option<Uuid>,
}

impl LivechatAuditLogFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.event.is_some()
            || self.actor.is_some()
            || self.subject_type.is_some()
            || self.subject_id.is_some()
            || self.company_id.is_some()
    }
}

/// Repository trait for LivechatAuditLog aggregate
///
/// This trait defines the persistence contract for LivechatAuditLog entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait LivechatAuditLogRepository: Send + Sync {
    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new livechat_audit_log entity
    async fn save(&self, entity: &LivechatAuditLog) -> Result<LivechatAuditLog>;

    /// Find livechat_audit_log by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<LivechatAuditLog>>;

    /// Find all livechat_audit_log entities
    async fn find_all(&self) -> Result<Vec<LivechatAuditLog>>;

    /// Update livechat_audit_log by ID
    async fn update(&self, id: &str, entity: &LivechatAuditLog)
        -> Result<Option<LivechatAuditLog>>;

    /// Delete livechat_audit_log by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List livechat_audit_log with pagination
    async fn list(
        &self,
        params: LivechatAuditLogPaginationParams,
    ) -> Result<LivechatAuditLogPaginatedResult>;

    /// List livechat_audit_log with pagination and filters
    async fn list_with_filters(
        &self,
        params: LivechatAuditLogPaginationParams,
        filters: LivechatAuditLogFilter,
    ) -> Result<LivechatAuditLogPaginatedResult>;

    /// Count all livechat_audit_log entities
    async fn count(&self) -> Result<u64>;

    /// Count livechat_audit_log entities matching filters
    async fn count_with_filters(&self, filters: LivechatAuditLogFilter) -> Result<u64>;

    /// Check if livechat_audit_log exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete livechat_audit_log by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted livechat_audit_log by ID
    async fn restore(&self, id: &str) -> Result<Option<LivechatAuditLog>>;

    /// List soft-deleted livechat_audit_log entities
    async fn list_deleted(
        &self,
        params: LivechatAuditLogPaginationParams,
    ) -> Result<LivechatAuditLogPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save livechat_audit_log entities
    async fn bulk_save(&self, entities: &[LivechatAuditLog]) -> Result<Vec<LivechatAuditLog>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
