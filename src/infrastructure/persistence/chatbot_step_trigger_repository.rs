//! Repository for ChatbotStepTrigger entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<ChatbotStepTrigger, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::ChatbotStepTrigger;

/// Table name for ChatbotStepTrigger entities
pub const TABLE_NAME: &str = "livechat.chatbot_step_triggers";

/// Repository for ChatbotStepTrigger entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct ChatbotStepTriggerRepository(
    backbone_orm::GenericCrudRepository<ChatbotStepTrigger, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for ChatbotStepTriggerRepository {
    type Target = backbone_orm::GenericCrudRepository<ChatbotStepTrigger, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target {
        &self.0
    }
}

impl ChatbotStepTriggerRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(
    ChatbotStepTriggerRepository,
    ChatbotStepTrigger,
    soft_delete
);
