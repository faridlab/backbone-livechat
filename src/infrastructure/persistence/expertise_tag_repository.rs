//! Repository for ExpertiseTag entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<ExpertiseTag, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::ExpertiseTag;

/// Table name for ExpertiseTag entities
pub const TABLE_NAME: &str = "livechat.expertise_tags";

/// Repository for ExpertiseTag entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct ExpertiseTagRepository(
    backbone_orm::GenericCrudRepository<ExpertiseTag, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for ExpertiseTagRepository {
    type Target = backbone_orm::GenericCrudRepository<ExpertiseTag, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl ExpertiseTagRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(ExpertiseTagRepository, ExpertiseTag, soft_delete);
