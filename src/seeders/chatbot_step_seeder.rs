//! ChatbotStep seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial ChatbotStep data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// CHATBOTSTEP SEEDER
// ============================================================================

/// Seeder for chatbot_step
pub struct SeedChatbotStepSeeder;

impl SeedChatbotStepSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedChatbotStepSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedChatbotStepSeeder {
    fn name(&self) -> &'static str {
        "SeedChatbotStepSeeder"
    }

    fn order(&self) -> i32 {
        7
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if livechat.chatbot_steps table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM livechat.chatbot_steps")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/chatbot_step_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM livechat.chatbot_steps")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
