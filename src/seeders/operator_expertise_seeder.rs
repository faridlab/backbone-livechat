//! OperatorExpertise seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial OperatorExpertise data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// OPERATOREXPERTISE SEEDER
// ============================================================================

/// Seeder for operator_expertise
pub struct SeedOperatorExpertiseSeeder;

impl SeedOperatorExpertiseSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedOperatorExpertiseSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedOperatorExpertiseSeeder {
    fn name(&self) -> &'static str {
        "SeedOperatorExpertiseSeeder"
    }

    fn order(&self) -> i32 {
        13
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if livechat.operator_expertise table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM livechat.operator_expertise")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/operator_expertise_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM livechat.operator_expertise")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
