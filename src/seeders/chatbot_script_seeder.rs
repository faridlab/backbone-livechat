//! ChatbotScript seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial ChatbotScript data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// CHATBOTSCRIPT SEEDER
// ============================================================================

/// Seeder for chatbot_script
pub struct SeedChatbotScriptSeeder;

impl SeedChatbotScriptSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedChatbotScriptSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedChatbotScriptSeeder {
    fn name(&self) -> &'static str {
        "SeedChatbotScriptSeeder"
    }

    fn order(&self) -> i32 {
        6
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if livechat.chatbot_scripts table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM livechat.chatbot_scripts")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/chatbot_script_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM livechat.chatbot_scripts")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
