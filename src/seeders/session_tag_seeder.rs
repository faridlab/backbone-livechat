//! SessionTag seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial SessionTag data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// SESSIONTAG SEEDER
// ============================================================================

/// Seeder for session_tag
pub struct SeedSessionTagSeeder;

impl SeedSessionTagSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedSessionTagSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedSessionTagSeeder {
    fn name(&self) -> &'static str {
        "SeedSessionTagSeeder"
    }

    fn order(&self) -> i32 {
        17
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if livechat.session_tags table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM livechat.session_tags")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/session_tag_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM livechat.session_tags")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
