//! ChatbotScript REST handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::middleware::AuthContext;
#[cfg(feature = "auth")]
use backbone_auth::AuthMiddleware;

// Domain imports
use crate::application::service::{ChatbotScriptService, ServiceError};
use crate::domain::entity::*;

// DTO imports
use crate::presentation::dto::{
    ChatbotScriptResponseDto, CreateChatbotScriptDto, PatchChatbotScriptDto, UpdateChatbotScriptDto,
};

/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum ChatbotScriptError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for ChatbotScriptError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for ChatbotScriptError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "CHATBOTSCRIPT_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "CHATBOTSCRIPT_VALIDATION_ERROR"),
            Self::Database(_) => (
                StatusCode::INTERNAL_SERVER_ERROR,
                "CHATBOTSCRIPT_DATABASE_ERROR",
            ),
            Self::Internal(_) => (
                StatusCode::INTERNAL_SERVER_ERROR,
                "CHATBOTSCRIPT_INTERNAL_ERROR",
            ),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for ChatbotScript.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /chatbot_scripts | List with pagination |
/// | POST | /chatbot_scripts | Create new |
/// | GET | /chatbot_scripts/:id | Get by ID |
/// | PUT | /chatbot_scripts/:id | Full update |
/// | PATCH | /chatbot_scripts/:id | Partial update |
/// | DELETE | /chatbot_scripts/:id | Soft delete |
/// | POST | /chatbot_scripts/bulk | Bulk create |
/// | POST | /chatbot_scripts/upsert | Upsert |
/// | GET | /chatbot_scripts/trash | List deleted |
/// | POST | /chatbot_scripts/:id/restore | Restore |
/// | DELETE | /chatbot_scripts/empty | Empty trash |
/// | GET | /chatbot_scripts/:id/deleted | Get deleted by ID |
/// | DELETE | /chatbot_scripts/trash/:id | Permanent delete from trash |
/// | GET | /chatbot_scripts/count | Count active entities |
/// | GET | /chatbot_scripts/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(ChatbotScriptService::with_repository(repository));
/// let router = create_chatbot_script_routes(service);
/// ```
pub fn create_chatbot_script_routes(service: Arc<ChatbotScriptService>) -> Router {
    BackboneCrudHandler::<
        ChatbotScriptService,
        ChatbotScript,
        CreateChatbotScriptDto,
        UpdateChatbotScriptDto,
        ChatbotScriptResponseDto,
    >::routes(service, "/chatbot_scripts")
}

/// Create Axum router with only the read (GET) endpoints for ChatbotScript.
///
/// Safe for public, unauthenticated exposure (e.g., reference data).
/// Mutations must be served separately via `create_chatbot_script_write_routes`,
/// typically wrapped in an auth middleware layer.
pub fn create_chatbot_script_read_routes(service: Arc<ChatbotScriptService>) -> Router {
    BackboneCrudHandler::<
        ChatbotScriptService,
        ChatbotScript,
        CreateChatbotScriptDto,
        UpdateChatbotScriptDto,
        ChatbotScriptResponseDto,
    >::read_routes(service, "/chatbot_scripts")
}

/// Create Axum router with only the write (mutation) endpoints for ChatbotScript.
///
/// These routes must NOT be publicly exposed. Wrap them with an auth
/// middleware before nesting into the application router.
///
/// # This is unguarded generic CRUD, not a validated write path
///
/// These are plain create/update/patch/delete mutations over the entity row —
/// they bypass all business invariants. If the module exposes a validated write
/// service (e.g. a command router over its domain engine), serve THAT instead
/// for any mutation that must respect domain rules.
pub fn create_chatbot_script_write_routes(service: Arc<ChatbotScriptService>) -> Router {
    BackboneCrudHandler::<
        ChatbotScriptService,
        ChatbotScript,
        CreateChatbotScriptDto,
        UpdateChatbotScriptDto,
        ChatbotScriptResponseDto,
    >::write_routes(service, "/chatbot_scripts")
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_chatbot_script_routes<A: AuthMiddleware + Send + Sync + 'static>(
    service: Arc<ChatbotScriptService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_chatbot_script_routes(service).layer(middleware::from_fn(
        move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                let token = req
                    .headers()
                    .get(axum::http::header::AUTHORIZATION)
                    .and_then(|h| h.to_str().ok())
                    .and_then(|raw| {
                        raw.strip_prefix("Bearer ")
                            .or_else(|| raw.strip_prefix("bearer "))
                    })
                    .unwrap_or("");
                match auth.authenticate(token).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => (
                        axum::http::StatusCode::UNAUTHORIZED,
                        axum::Json(serde_json::json!({
                            "success": false,
                            "error": "unauthorized",
                            "message": "Authentication required"
                        })),
                    )
                        .into_response(),
                }
            }
        },
    ))
}
