//! Application service for ConversationTag entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Type alias over `GenericCrudService` — all CRUD logic lives in backbone-core.
//! For custom behaviour, create `conversation_tag_service_custom.rs` (preserved on regen).

use backbone_core::GenericCrudService;

use crate::domain::entity::ConversationTag;
use crate::infrastructure::persistence::ConversationTagRepository;
use crate::presentation::dto::{CreateConversationTagDto, UpdateConversationTagDto};

/// Application service for ConversationTag entities.
///
/// All 12 standard CRUD operations (list, create, get, update, patch,
/// soft_delete, restore, empty_trash, bulk_create, upsert, find_by_id,
/// list_deleted) are provided by `GenericCrudService`.
///
/// Custom extensions go in `conversation_tag_service_custom.rs`:
///
/// ```rust,ignore
/// pub struct ConversationTagServiceCustom {
///     inner: Arc<ConversationTagService>,
///     // add domain-specific dependencies here
/// }
/// ```
pub type ConversationTagService = GenericCrudService<
    ConversationTag,
    CreateConversationTagDto,
    UpdateConversationTagDto,
    ConversationTagRepository,
>;

// <<< CUSTOM
// END CUSTOM
